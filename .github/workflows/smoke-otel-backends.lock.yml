# RETIRED agentic Smoke OTEL — Copilot secret hard-fail.
# SSOT: smoke-otel-write-agentless.yml (HTTP 200 WRITE_OK proven run 35161680890)
name: "Smoke OTEL (retired)"
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    runs-on: ubuntu-slim
    steps:
      - run: |
          echo "RETIRED: agentic Smoke OTEL requires COPILOT_GITHUB_TOKEN (paid/fail)."
          echo "Use workflow: Smoke OTEL Write (agentless)"
          echo "https://github.com/${GITHUB_REPOSITORY}/actions/workflows/smoke-otel-write-agentless.yml"
